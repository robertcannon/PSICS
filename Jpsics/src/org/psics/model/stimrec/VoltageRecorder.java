package org.psics.model.stimrec;
 
import org.psics.quantity.annotation.ModelType;

@ModelType(info = "Records the potential at a point on the cell", standalone = false, 
		tag = "Voltage recorder", usedWithin = { Access.class })
public class VoltageRecorder extends DisplayableRecorder {

	 
	
	
}
